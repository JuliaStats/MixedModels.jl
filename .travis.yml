language: julia
os:
  - linux
  - osx
  - windows
julia:
  - 1.0
  - 1.2
  - 1.3
codecov: true
notifications:
  email: false
jobs:
  allow_failures:
    - julia: 1.3
  fast_finish: true
  include:
    - stage: "Documentation"
      os: linux
      julia: 1.2
      script:
        - julia --project=docs/ -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd()));
                                               Pkg.instantiate()'
        - julia --project=docs/ docs/make.jl
      after_success: skip
